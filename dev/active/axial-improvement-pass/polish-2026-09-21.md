# Playtest polish — 2026-09-21

Scope: Caden's six screenshot references and feedback after playing Max. Preserve the search engine and existing dirty improvement pass.

## Decisions and changes

- Replace the local/AI result's spatial winning-line prose with Moves and Match time. Keep the existing result actions and board-line rendering.
- Record wall-clock first-placement and final-placement timestamps in the existing version-1 saved match, preserving old saves. Reload and completed-match review retain the duration. New matches reset it. Older matches without timestamps omit time rather than inventing a value. Time includes time away between the first and last move; it is elapsed match time, not an active-attention timer.
- Start AI search immediately; only hold early results until 650 ms after the turn starts. Searches exceeding that time receive no extra delay. Undo, reset, leaving the AI series and destruction cancel pending presentation. Search choices and budgets are unchanged.
- Preserve redo history during review, including AI-opening matches. Do not queue a fresh AI search while recorded redo moves remain.
- Remove both player-band toruses and the triangular wireframe overlay identified in the piece screenshot. Keep the base geometry/material, highlights and animation. Appearance uses P1/P2 color labels and asks for distinct colors when selected colors are too similar.
- Insert Online controls immediately below the Local/AI/Online switch using a Svelte snippet in the existing Match panel. Active rooms retain their controls below the locked summary.
- Rewrite tutorial slides 1, 2 and 7; remove band references and em dashes. Delete the unused spatial-description helpers/tests after removing that result feature; retain line-score counting and its tests.

## Verification

- New controller lifecycle tests cover fast/slow AI replies, cancellation, reset races, saved match duration, legacy saves and AI-opening review.
- New browser checks cover fixed Online-switch placement at 1440/390 px and result stats/reload at 1280/320 px.
- Final workspace type checks/lint and all 137 web unit tests passed. Fresh production builds passed with the existing chunk-size advisory.
- The scoped production browser run passed 31/33; both new result checks failed only because their initial combined-text assertion omitted rendered whitespace. Replaced that with separate label/value assertions. A fresh build and focused home/AI lifecycle, Online-layout and result run then passed 10/10. All 33 scoped cases have passing evidence across those runs; this is not a claim of one complete 33/33 run. The known time-sensitive multiline AI test-contract issue was outside this scoped run and remains open.
- Inspected captured result screenshots at 1280×800 and 320×568: moves, duration and all result actions fit without clipping.
- In-app inspection used a separate localhost origin so the user's active 127.0.0.1 match was preserved. Smooth pieces and updated appearance labels were inspected; no browser warning/error logs were captured.

The earlier dependency, abuse-protection, sharing metadata and AI test-contract review findings remain separate release work. No commit, merge or deployment is included in this polish pass.


## Tutorial and controls follow-up

- Clarify the opening as Connect 4 in 3D space with a few fun twists. Explain straight connections across, up and diagonally. Slide 2 introduces completing a row of three; remove the extra unchanged-match sentence. No em dashes in tutorial copy.
- Replace the staged selection exercise with a separate canonical practice game: six legal setup moves leave Player 1 three in a row, with the open winning space highlighted. An incorrect column gives a hint; the correct drop actually connects four and renders the completed line. Continue requires the win. Practice never writes the saved match or score, and the scene restores the real board on continue/exit. Main match controls are inert during practice.
- Use a single animation-frame owner for the automatic tutorial border sweep, queue hover coordinates while it plays, then hand off smoothly. Preserve reduced-motion behavior and cancellation on step changes/unmount.
- Remove Crystal from choices, rendering and previews. Existing saved Crystal preferences resolve to Cube. Cube and Orb fill the two-column shape selector. The dense draw regression uses Orb.
- Scroll the entire settings panel, including the toolbar. Preserve mobile half/full sheets and collapse/reopen. Tutorial spotlight clipping uses the expanded panel only so the collapsed toggle retains its padded outline.

Validation: workspace check/lint passed; all 140 web unit tests passed. Manual in-app inspection confirmed the prepared row, actual fourth-piece win, rendered line and restoration of the existing two-move match. Browser regression tests cover wrong/correct practice drops, canonical rendered piece/line counts, reentry, early exit and saved-match preservation with nondefault board/rules; separate tests cover sweep/hover continuity and toolbar scrolling. A fresh production build and the final combined production-preview browser run passed 27/27 with one worker and no retries (tutorial, practice isolation, glow, toolbar scrolling, Online layout, responsive UI and game-feel regressions). The build retains its existing large-chunk advisory. The initial integration run caught and fixed collapsed-toggle spotlight clipping; the practice test was corrected to expand the menu before reopening Help and given a 60-second budget for its repeated software-rendered scene mounts. Final focused lint and git diff whitespace checks passed. The dev server remains available at port 5173. No commit, merge or deployment was made.


## Fuller practice position and UI consistency

- Practice now starts from 24 legal alternating moves, with nine elevated pieces in stacks two to three high. The original fourth-piece target remains the only immediate Player 1 win. Unit coverage checks every setup prefix is unfinished and every other column remains nonwinning.
- Put scrolling in an inset `.panel-scroll` inside the rounded settings shell. Desktop reserves a stable gutter in both states, keeping all six toolbar icon x coordinates identical. Portrait retains native touch scrolling with the rail hidden, keeping full touch targets and existing positions. Tutorial spotlight scrolling/clipping follows the new scroller.
- The UI previously requested Inter without serving a font file, leaving its actual typeface to system fallback. Bundle the upright Inter Variable 4.1 WOFF2 locally as `Axial UI`, preload it, and include its SIL Open Font License. Official source: https://rsms.me/inter/. The asset is about 344 KiB, served once from the app origin with no third-party font requests or new package dependency.
- Apply the same UI family to forms, redirects, dialogs and canvas board labels; preserve the original brand stack, weight and spacing. Replace near-identical arbitrary weights with a 400/500/600/700 scale. Canvas labels wait for fonts before texture creation and remain hidden until ready, preventing untextured white sprites on slow downloads.
- Release tutorial control of the panel after Svelte synchronizes its restored state instead of waiting 320 ms. An immediate Expand click after tutorial exit now works.

Validation: workspace check/lint and all 141 web unit tests passed. Desktop/phone typography tests verify the bundled face actually loads, shared family/weight use, preserved brand stack and no remote font requests. An additional deliberately delayed font request verifies hidden untextured sprites and visible textured labels after loading. Manual in-app inspection covered the fuller practice board, unified tutorial type and inset desktop scrollbar.

The combined production-preview run passed 31/31 (one worker, no retries). After the final cold-font sprite visibility guard, a fresh production build and all three font regressions passed 3/3, including the delayed request. Final Svelte check reported zero errors/warnings, focused lint and git diff whitespace checks passed. Builds retain the pre-existing large-chunk advisory. The preview remains at http://127.0.0.1:5173/; no commit, merge or deployment was made.


## Tutorial target and scrollbar follow-up

- Add a persistent, bright filled tile with a contrasting square outline at the tutorial's winning column. It remains fixed when hovering/keyboard-selecting another column and disappears after the winning drop. The cue uses static geometry and respects reduced motion without an extra animation loop. Instructions now explicitly mention the glowing tile.
- Hide the native settings scrollbar on all viewports with standard and WebKit CSS; keep wheel, touch and keyboard scrolling. Remove the desktop gutter as it is no longer needed.
- Give the desktop scroller internal vertical clearance for the existing 1px icon lift while compensating outer padding to retain toolbar position. Compact portrait controls use the existing stationary hover treatment in both expanded and collapsed states, retaining their touch dimensions. Toolbar focus outlines sit inside the buttons.
- Extend browser tests to check hidden rails, continued scrolling, stable desktop positions, hover clearance in expanded/collapsed states, and the tutorial cue's persistence/removal in actual Three renders.

Type checks, workspace lint and the fresh production build passed. Final scoped production-preview browser run passed 12/12 with one worker and no retries (practice, tutorial, toolbar hover/scroll, responsive controls and Online layout). Both desktop and compact-phone hovered-pill screenshots were inspected alongside the highlighted board. The extra collapsed-phone hover check caught constrained internal padding; the final run includes its fix. The build retains the existing large-chunk advisory. No commit, merge or deployment was made.

## Themed hints and compact layouts

- Replace native control-title bubbles with one shared tooltip using the app's font, surface, border and text colors. Hover waits 600 ms, keyboard focus shows the hint immediately, and touch taps do not leave hints behind. Position within the viewport and dismiss on leave, Escape, interaction, scrolling or resize. Preserve accessible button labels and connect displayed descriptions with `aria-describedby`.
- Defer focus-event updates until after Svelte finishes rendering. Tutorial teardown can synchronously blur a focused button; handling that blur immediately caused a reactive-state error, now covered by the tutorial lifecycle checks.
- Remove the mobile toolbar's nested capsule and icon separators. Use individual circular buttons, animate the outer sheet's dimensions and keep its body width stable during expansion. Preserve the compact three-button collapsed state and six expanded controls.
- Reserve enough horizontal room for the settings panel before positioning the turn chip, avoiding overlap around intermediate laptop/tablet widths. Keep it centered when there is sufficient room.
- Constrain tutorial cards to the available height, keep navigation outside the scrollable copy, narrow cards beside landscape spotlights, and tune the smallest phone typography. Tutorial and practice text cannot be selected. Compact tutorial navigation uses 44px targets.
- Stack Tactical's Coming soon badge below its label so it also fits narrow desktop settings panels.

Visual inspection includes light/dark hints, mobile circular controls, intermediate-width header spacing, and tutorial screenshots at 320×568, 390×844, 844×390, 1024×600 and 1280×720. The retained viewport matrix checks all seven steps and a successful practice drop, complete visible copy, card/action bounds, spotlight separation, nonselectable text and browser errors. A separate in-app preview confirmed the themed keyboard hint without modifying the user's active match.

Final validation: Svelte check reported zero errors/warnings, lint passed, and all three practice-fixture unit tests passed. A fresh production build and the combined scoped browser suite passed **29/29**, one worker with no retries (2.9 minutes): hints, responsive controls, toolbar scrolling/hover, complete tutorial layouts, sweep behavior, practice isolation and Online placement. The agent also checked stationary-board behavior through panel toggles, 2/2 passing. The existing large-chunk build advisory remains. Production screenshots are under `/tmp/axial-final-ui-results/`; these are emulated browser viewports, not physical-device validation. The development server remains at http://127.0.0.1:5173/. No commit, merge or deployment was made.

## Final transient-overflow follow-up

- Hide native rails on both tutorial scrolling containers while preserving overflow scrolling. Typing and the paragraph's entrance transform can briefly create overflow before the finished copy fits; the rail no longer flashes during that interval.
- Retain the settings scroller's 2px vertical clearance on expanded compact panels. Only the stationary collapsed phone toolbar removes that padding, so the expanded 1px hover lift stays inside the scroll clip.
- Strengthen hover checks by waiting for the button's transition to finish before measuring clearance. The former polling assertion could pass at the unshifted starting frame. Add 320px and 540px cases and a tutorial typing-phase scrollbar check.

Both new regressions failed against the preceding version and passed after the CSS fixes. Inspected the light tutorial during typing and the expanded 540px toolbar with an actively hovered button. The change preserves the approved layout and animation.

Final session gate: Svelte check (zero errors/warnings), lint and whitespace checks passed. Fresh production build plus all **32/32 scoped browser checks** passed with one worker and no retries (3.2 minutes), including every tutorial step at five viewport sizes and settled hover checks at 320, 390, 540 and 1440px. Screenshot evidence is under `/tmp/axial-session-final-results/`, `/tmp/axial-tutorial-typing.png` and `/tmp/axial-toolbar-expanded-hover-*.png`. The existing build chunk-size advisory remains. Development preview still returns HTTP 200 on port 5173. Changes remain uncommitted and undeployed; separate release findings remain open.
